It looks like you already have the `greet()` endpoint in **app/main.py**, so no further edits are needed:

```python
@app.get("/greet")
def greet(name: str = "world"):
    return {"message": f"Hello, {name}!"}
```
【F:app/main.py†L13-L15】